logging:
  level:
    com:
      example:
        mapper:
          UserMapper: debug
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/javaweb?serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: 123456
  cache:
    redis:
      use-key-prefix: true